{
  "name": "qh-ui",
  "type": "module",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "packageManager": "pnpm@10.7.1",
  "devDependencies": {
    "@antfu/eslint-config": "^4.16.2",
    "eslint": "^9.30.1",
    "lint-staged": "^16.1.2",
    "rimraf": "^6.0.1",
    "typescript": "^5.8.3"
  },
  "lint-staged": {
    "*": "eslint --fix"
  }
}
